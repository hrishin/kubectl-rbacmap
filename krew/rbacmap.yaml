apiVersion: krew.googlecontainertools.github.com/v1alpha2
kind: Plugin
metadata:
  name: rbacmap
spec:
  version: "v0.1.0" # This will be managed by Git Tags
  homepage: https://github.com/hrishis/kubectl-rbacmap
  shortDescription: "Map RBAC permissions for subjects (ServiceAccounts, Users, Groups)"
  description: |
    kubectl-rbacmap finds all permissions associated with Kubernetes subjects
    by inspecting Roles, ClusterRoles, RoleBindings, and ClusterRoleBindings.
    It resolves complex mappings (like AWS IAM ARNs to EKS identities) and
    outputs the exact access granted in clean tables, markdown, or CSV.
  platforms:
  - selector:
      matchLabels:
        os: darwin
        arch: amd64
    # The URI will be your GitHub release URL, like:
    uri: https://github.com/hrishis/kubectl-rbacmap/releases/download/v0.1.0/kubectl-rbacmap_Darwin_x86_64.tar.gz
    sha256: "YOUR_SHA256_HERE"
    bin: kubectl-rbacmap
  - selector:
      matchLabels:
        os: darwin
        arch: arm64
    uri: https://github.com/hrishis/kubectl-rbacmap/releases/download/v0.1.0/kubectl-rbacmap_Darwin_arm64.tar.gz
    sha256: "YOUR_SHA256_HERE"
    bin: kubectl-rbacmap
  - selector:
      matchLabels:
        os: linux
        arch: amd64
    uri: https://github.com/hrishis/kubectl-rbacmap/releases/download/v0.1.0/kubectl-rbacmap_Linux_x86_64.tar.gz
    sha256: "YOUR_SHA256_HERE"
    bin: kubectl-rbacmap
  - selector:
      matchLabels:
        os: linux
        arch: arm64
    uri: https://github.com/hrishis/kubectl-rbacmap/releases/download/v0.1.0/kubectl-rbacmap_Linux_arm64.tar.gz
    sha256: "YOUR_SHA256_HERE"
    bin: kubectl-rbacmap
  - selector:
      matchLabels:
        os: windows
        arch: amd64
    uri: https://github.com/hrishis/kubectl-rbacmap/releases/download/v0.1.0/kubectl-rbacmap_Windows_x86_64.zip
    sha256: "YOUR_SHA256_HERE"
    bin: kubectl-rbacmap.exe
